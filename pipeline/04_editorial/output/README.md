# Scaffolding only

Real output for a run is written to `runs/<run-id>/04_editorial/output/` — the edited modules plus
an `editorial-report.md`. This folder is a template marker, kept so the stage structure is visible
in git.
