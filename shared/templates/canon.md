# Canon — <Course Title>

The course's consistency contract. Written once at the curriculum stage, **binding** on every
authoring sub-agent and on the editorial stage. Its job is to stop blind-parallel authors from
drifting apart. Be decisive — one call per item, no "TBD"s.

## 1. Symbol & naming registry

One symbol and one name for every recurring object. List the decision and any legitimate exceptions.

| Thing | Canonical symbol / name | Notes / legitimate exceptions |
|-------|-------------------------|-------------------------------|
| Generic operation | `$*$` | per-structure symbols are fine (e.g. `$+$` for one family, `$\times$` for another) |
| … | … | … |

## 2. "Already-taught" ledger & dedup plan

Each shared fact has **one owner module** that gives the full treatment; every later use is a
one-line **callback**, never a re-derivation. (A reminder is good; a fresh re-derivation is the bug.)
At **`intro`** level the callback must **re-gloss** the term in a few words ("the kernel — the
random-edit sampler from module 9"), not merely name it; a beginner has forgotten. (See
`_config/course-design.md` §2, "The level dial".)

| Fact | Owner (full treatment) | Callback in | Suggested callback line |
|------|------------------------|-------------|-------------------------|
| … | module NN | NN, NN | "the … we established in module NN" |

## 3. Metaphor-ownership map

One running image per module; other modules reference it, they don't reinvent it.

| Image / metaphor | Owner module |
|------------------|--------------|
| … | NN |

**Ownership is not endorsement.** A metaphor earns a row here only if it passes the
referent/payoff/bridge test: it points at exactly one correct real-world referent with no
misleading association (a "machine with slots and a crank" wrongly evokes a slot machine —
randomness — so it fails), it can be paid off (developed and reused, not announced then dropped),
and it can be cashed into the formal content via the intuition→definition bridge. A candidate image
that fails any of these is **cut** — replaced with a clean concrete shape or with no metaphor — not
assigned an owner. (See `_config/voice/editorial-craft.md` and `_config/math-style.md` §1.)

## 4. Stock-phrase caps

Phrases that go limp through repetition. Keep the single strongest instance; cut or vary the rest.

| Phrase | Cap | Keeper module |
|--------|-----|---------------|
| … | 1 total | NN |

## 5. Voice target

One short paragraph fixing the narrator: register, warmth, sentence rhythm — so all modules read as
one person. Name the current drift to correct (e.g. "modules X–Y read dry; pull them toward the warm,
concrete storyteller of module Z").
