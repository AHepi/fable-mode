# Media assets

Audio and video files for courses live here and are served at `/<base>/media/...`.
Reference them with the site's `url()` helper in a component, or an inline
`<audio controls>` / `<video controls>` element. (Images are co-located in each
course folder under `courses/<slug>/assets/` instead, so Astro can optimize them.)
